{% extends '::layout_admin.html.twig' %}

{% block titulo %}Edicion del Movil{% endblock %}

{% block contenedor %}
	<div class="span12">
				<div class="span3">
					<ul class="nav nav-list well">
						<li class="nav-header">
							List header
						</li>
						<li class="active">
							<a href="{{ path('movil') }}">Lista Moviles</a>
						</li>
						<li>
							<a href="{{ path('movil_new') }}">Crear Nuevo</a>
						</li>
						<li class="divider">
						</li>
						<li>
							<a href="#">Help</a>
						</li>
					</ul>
				</div>
				<div class="span9">
				<table class="table table-striped">
					<form action="{{ path('movil_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
					    {{ form_widget(edit_form) }}
					    <div>
							<button type="submit" class="btn btn-info" value="Guardar">Guardar</button>
							 <form action="{{ path('movil_delete', { 'id': entity.id }) }}" method="post">
							  <button type="submit" class="btn btn-warning" value="Eliminar">Eliminar</button>
							  </form>
						</div>
					</form>
				</table>
				</div>
			</div>
	</div>
{% endblock %}